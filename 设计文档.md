# nano-vllm KV Cache Swap 设计文档

## 调度优先级：nano-vllm vs vLLM

### nano-vllm：prefill 优先

`scheduler.py` 的 `schedule()` 先尝试 prefill，有 prefill 就直接返回，不走 decode：

```python
def schedule(self):
    # 1. prefill
    while self.waiting:
        if not self.block_manager.can_allocate(seq):
            break
        self.block_manager.allocate(seq)
        scheduled_seqs.append(seq)
    if scheduled_seqs:
        return scheduled_seqs, True, [], []   # 有 prefill 直接返回

    # 2. swap-in + decode（只有没有 prefill 时才走到这里）
    ...
```

- prefill 和 decode 不会混在同一个 batch
- 有新请求等待 prefill 时，所有 running 序列暂停 decode
- 优点：降低首 token 延迟（TTFT）
- 缺点：持续有新请求时，decode 序列被反复暂停

### vLLM v1：decode 优先

`vllm/v1/core/sched/scheduler.py` 的 `schedule()` 先处理 running（decode），再处理 waiting（prefill）：

```python
def schedule(self):
    # 1. 先处理 RUNNING 序列（decode）
    for request in self.running:
        new_blocks = self.kv_cache_manager.allocate_slots(request, ...)
        if new_blocks is None:
            preempted_req = self.running.pop()
            self._preempt_request(preempted_req)

    # 2. 只有没有抢占时才调度新请求（prefill）
    if not preempted_reqs:
        for request in self.waiting:
            if allocation_fails:
                break  # 不会抢占 running 序列
```

- running 之间可以互相抢占，但 waiting 永远不会触发抢占
- 有抢占发生时不调度新请求（`if not preempted_reqs`）
- 支持 chunked prefill，prefill 和 decode 可以混 batch
- 优点：保护已有请求，避免饥饿
- 缺点：新请求首 token 延迟可能较高

### 对比总结

| | nano-vllm | vLLM v1 |
|---|---|---|
| 调度优先级 | prefill > decode | decode > prefill |
| prefill 能否抢占 decode | 不能（但会阻塞 decode） | 不能 |
| decode 能否抢占 decode | 能（swap-out 到 CPU） | 能（preempt 回 waiting） |
| prefill 和 decode 混 batch | 不能 | 能（chunked prefill） |
| 为 prefill swap-out decode | 不会 | 不会 |

## Swap 在调度流程中的位置

### Scheduler（决策层，只操作元数据）

```
schedule() {
    prefill 阶段: 不涉及 swap
    ↓
    swap-in 阶段: 检查 swapped 队列，GPU 有空间则生成 swap-in mappings
    ↓
    decode 阶段: blocks 不够时 preempt → 生成 swap-out mappings
    ↓
    返回 (seqs, is_prefill, swap_in_mappings, swap_out_mappings)
}
```

Scheduler 调用 `block_manager.swap_out()` 时只修改元数据（释放 GPU block ID，分配 CPU block ID），不做实际 tensor 拷贝。

### Runner（执行层，操作 tensor）

```
run(seqs, is_prefill, swap_in, swap_out) {
    1. swap-out: GPU → CPU  （先保存即将被覆盖的 block 数据）
    2. swap-in:  CPU → GPU  （恢复之前换出的序列）
    3. 推理                  （往复用的 blocks 写入新 KV）
}
```

**swap-out 必须在推理前执行**，因为 scheduler 在调度阶段已将被抢占序列的 GPU blocks 释放回 free pool，这些 blocks 可能被其他序列复用。推理时会往这些 blocks 写入新 KV 数据，如果在推理后才做 GPU→CPU 拷贝，拷到的是被覆盖后的错误数据。

### 完整 step 时序

```
LLMEngine.step():
    scheduler.schedule()        ← 元数据决策
    model_runner.run()          ← swap-out → swap-in → 推理
    scheduler.postprocess()     ← 追加 token、判断结束
```

## CUDA Swap Kernel

### 实现

GPU↔CPU 的 KV cache block 拷贝通过自定义 CUDA kernel 实现（`nanovllm/csrc/swap_kernels.cu`），使用 zero-copy 直接访问 CPU pinned memory，一次 kernel launch 完成所有 block 的拷贝。

kernel 配置：`grid=(N, 4), block=(128)`，其中 N 为本次要搬运的 block 数量。每个 KV cache block 的数据由 4 个 CUDA block（共 512 线程）分段并行拷贝，每线程每次搬运 16 bytes（int4）。

### 性能

测试环境：A100-SXM4-40GB，Qwen3-0.6B（28 layers, 8 kv_heads, 128 head_dim, block_size=256, bf16），单个 KV cache block 大小 28 MB。

|  | copy_()（原始） | CUDA kernel grid=(4,1) | CUDA kernel grid=(N,4) |
|---|---|---|---|
| Swap-out 带宽 | 1.96 GB/s | 6.74 GB/s | 19.48 GB/s |
| Swap-in 带宽 | 3.23 GB/s | 4.18 GB/s | 22.06 GB/s |
| 总 swap 耗时 | 3337 ms | 1579 ms | 394 ms |
| 占推理总时间 | 3.8% | 1.7% | 0.5% |

grid=(N,4) 的关键优化：每个 mapping 用 4 个 CUDA block 并行搬运，即使 N=1 也有 512 线程并发访问 PCIe，足以打满带宽。grid_y 从 2 到 4 有明显提升，4 以上基本持平。

### 限制

- 以上性能数据仅在 28 MB block 大小（Qwen3-0.6B, block_size=256, bf16）下测试验证。不同模型配置（层数、kv_heads 数、head_dim、dtype）会改变 block 大小，最优的 `BLOCKS_PER_MAPPING` 参数可能不同。对于显著更小的 block，可能需要减少 grid_y 以避免线程过多导致的调度开销；对于更大的 block，可能需要增加 grid_y。
- `block_size_in_bytes` 必须是 16 字节对齐的（int4 宽度）。
