# Pending Block 状态机实验

## 实验目的

在不修改正式 `BlockManager` 的前提下，验证异步 KV swap 所需的两阶段资源生命周期，确保 CUDA 搬运完成前 GPU 和 CPU blocks 不会被提前复用。

## 状态设计

Block 使用四种互斥状态：

```text
FREE
USED
PENDING_FREE
PENDING_WRITE
```

请求使用：

```text
RUNNING
SWAPPING_OUT
SWAPPED
SWAPPING_IN
```

每个 `PendingSwap` 记录请求、方向、GPU blocks、CPU blocks、Event 和操作状态。操作只能从 `PENDING` 进入 `COMMITTED` 或 `ABORTED`，结束后从 active pending 集合移除。

## 两阶段转换

Swap-out reserve：

```text
GPU: USED → PENDING_FREE
CPU: FREE → PENDING_WRITE
请求: RUNNING → SWAPPING_OUT
```

Swap-out commit：

```text
GPU: PENDING_FREE → FREE
CPU: PENDING_WRITE → USED
请求: SWAPPING_OUT → SWAPPED
```

Swap-in reserve：

```text
CPU: USED → PENDING_FREE
GPU: FREE → PENDING_WRITE
请求: SWAPPED → SWAPPING_IN
```

Swap-in commit：

```text
CPU: PENDING_FREE → FREE
GPU: PENDING_WRITE → USED
请求: SWAPPING_IN → RUNNING
```

Abort 执行相反转换，恢复传输开始前的有效副本和请求状态。

## 不变量

- 每个 block 在任意时刻只有一个状态和一个所有者。
- `FREE` block 没有所有者，所有非 `FREE` block 必须有所有者。
- Pending operation 中记录的 blocks 必须处于对应 pending 状态并属于该请求。
- Pending blocks 不计入可分配数量。
- 已完成或回滚的操作不能重复提交。
- 不同 Event 可以乱序完成，不需要等待队头操作。

## 测试结果

```text
test_abort_swap_in_restores_swapped_request ... ok
test_abort_swap_out_restores_running_request ... ok
test_completed_operations_are_reclaimed_out_of_order ... ok
test_operation_cannot_finish_twice ... ok
test_pending_blocks_cannot_be_reserved_again ... ok
test_swap_in_blocks_stay_unavailable_until_commit ... ok
test_swap_out_blocks_stay_unavailable_until_commit ... ok

Ran 7 tests
OK
```

## 复现

```bash
source .venv/bin/activate
python study/pending_blocks/test_pending_blocks.py
```

## 限制与下一步

当前使用可控的 `FakeEvent`，只验证 CPU 元数据状态机，没有执行 CUDA 搬运，也没有修改正式推理路径。下一步应把真实 `torch.cuda.Event` 接入最小 GPU 集成实验：在 copy stream 提交 `copy_blocks_2d`，Event 完成前验证 blocks 不可复用，完成后提交状态并核对数据。通过后再评估如何处理 tensor parallel 下多个 rank 的完成事件。
